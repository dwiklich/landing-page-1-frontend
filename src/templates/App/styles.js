import styled, { css } from 'styled-components';

export const Container = styled.div`
  /* ${(props) => css`
    background-color: ${props.color ? props.color : 'purple'};
  `} */

  /* ${({ theme, color }) => css`
    background-color: ${theme.colors.mainBg};
    color: ${color};
  `} */
`;
